# taskcli

Gestionnaire de tâches en ligne de commande, écrit en Python.

## Installation

```bash
git clone git@github.com:emilymargaux33/taskcli.git
cd taskcli
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
